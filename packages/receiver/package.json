{
  "name": "@webhook-proxy/receiver",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop webhook-receiver",
    "pm2:restart": "pm2 restart webhook-receiver",
    "pm2:logs": "pm2 logs webhook-receiver",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@webhook-proxy/shared": "workspace:*",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
