{
  "name": "webhook-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Webhook proxy system with validator (Hetzner) and receiver (Mac Mini)",
  "scripts": {
    "build": "pnpm -r build",
    "dev:validator": "pnpm --filter validator dev",
    "dev:receiver": "pnpm --filter receiver dev",
    "clean": "pnpm -r clean"
  },
  "keywords": ["webhook", "proxy", "stripe"],
  "author": "owen-raum",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
